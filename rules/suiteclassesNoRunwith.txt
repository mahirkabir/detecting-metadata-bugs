Rule suitclasses-no-runwith {
	for (class c in getClasses()) {
		if (hasAnnotation(c, "@SuiteClasses")) {
			assert(hasAnnotation(c, "@RunWith")) {
				msg("Class: %s has @SuiteClasses but no @RunWith", getFQN(c));
			}
		}
	}
}